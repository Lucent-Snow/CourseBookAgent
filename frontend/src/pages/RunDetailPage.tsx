import { useEffect, useState } from 'react'
import { AlertTriangle, ArrowLeft, Bot, Check, Clock3, Pause, RefreshCw } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { Button } from '@/components/ui/button'
import { api } from '@/api/client'
import { productApi } from '@/api/product'
import type { RunProjection } from '@/product-types'

const agentTone = { pending: 'text-[#829092] bg-[#f1f3f3]', running: 'text-[#147d86] bg-[#e6f3f3]', succeeded: 'text-[#27774a] bg-[#e9f5ee]', failed: 'text-red-700 bg-red-50', blocked: 'text-amber-700 bg-amber-50' }
export function RunDetailPage() {
  const { runId = '' } = useParams()
  const [run, setRun] = useState<RunProjection | null>(null)
  const [error, setError] = useState('')
  async function load() { try { setRun(await productApi.run(runId)) } catch (err) { setError((err as Error).message) } }
  useEffect(() => {
    const refresh = () => { void productApi.run(runId).then(setRun).catch((err) => setError((err as Error).message)) }
    refresh()
    const timer = window.setInterval(refresh, 1200)
    return () => window.clearInterval(timer)
  }, [runId])
  async function action(kind: 'retry'|'cancel') { try { if (kind === 'retry') await api.retryJob(runId); else await api.cancelJob(runId); await load() } catch (err) { setError((err as Error).message) } }
  if (!run) return <div className="p-10 text-sm text-[#718183]">{error || '正在读取运行状态…'}</div>
  return <div className="mx-auto max-w-[1180px] px-4 py-6 md:px-10 md:py-8"><header className="flex items-start justify-between"><div><Link to="/runs" className="inline-flex items-center gap-2 text-sm text-[#718183]"><ArrowLeft size={15} />返回运行中心</Link><h1 className="mt-5 text-[25px] font-semibold">课程 {run.course_id} · 课程教辅书</h1><p className="mt-2 font-mono text-xs text-[#819092]">{run.run_id}{run.snapshot_id && ` · 输入快照 ${run.snapshot_id}`}</p></div><div className="flex gap-2">{['failed','partial','interrupted'].includes(run.status) && <Button variant="outline" onClick={() => void action('retry')}><RefreshCw size={14} />恢复或重试</Button>}{['queued','running'].includes(run.status) && <Button variant="outline" onClick={() => void action('cancel')}><Pause size={14} />停止运行</Button>}{run.artifact_available && <Button render={<Link to={`/artifacts/${run.run_id}`} />} className="bg-[#147d86] text-white">查看产物</Button>}</div></header>{error && <div className="mt-5 rounded-md border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="mt-7 grid gap-5 lg:grid-cols-[1fr_340px]"><main className="space-y-5"><section className="rounded-lg border border-[#dfe6e6] bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="text-base font-semibold">{run.message || '运行准备中'}</h2><p className="mt-1 text-xs text-[#718183]">当前阶段：{run.phase}</p></div><strong className="text-2xl text-[#147d86]">{run.progress}%</strong></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-[#e7eeee]"><div className="h-full rounded-full bg-[#147d86] transition-all" style={{ width: `${run.progress}%` }} /></div><div className="mt-3 flex gap-5 text-[11px] text-[#718183]"><span>{run.active_agents} 个 Agent 运行</span><span>{run.failed_agents} 个异常</span><span>{run.total_agents} 个章节任务</span></div></section><section className="overflow-hidden rounded-lg border border-[#dfe6e6] bg-white"><div className="flex items-center justify-between border-b border-[#edf0f0] px-5 py-4"><h2 className="text-sm font-semibold">章节 Agent</h2><span className="text-xs text-[#819092]">结构化任务状态</span></div>{run.agents.length ? <div className="divide-y divide-[#edf0f0]">{run.agents.map((agent) => <div key={agent.agent_id} className="flex items-center gap-3 px-5 py-4"><span className={`grid size-9 place-items-center rounded-md ${agentTone[agent.status]}`}><Bot size={16} /></span><div className="min-w-0 flex-1"><p className="text-sm font-medium">{agent.label}</p><p className="mt-1 truncate text-[11px] text-[#718183]">{agent.message}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-medium ${agentTone[agent.status]}`}>{agent.status}</span>{agent.status === 'succeeded' && <Check size={15} className="text-[#27774a]" />}{agent.retryable && <RefreshCw size={14} className="text-red-600" />}</div>)}</div> : <div className="py-14 text-center text-sm text-[#718183]">任务将在读取讲次后出现</div>}</section></main><aside className="space-y-5"><section className="rounded-lg border border-[#dfe6e6] bg-white p-5"><h2 className="text-sm font-semibold">运行信息</h2><dl className="mt-4 space-y-3 text-xs">{[['状态',run.status],['工作流',run.preset_id],['课程 ID',run.course_id],['更新时间',run.updated_at ? new Date(run.updated_at).toLocaleString() : '—']].map(([label,value]) => <div key={label} className="flex justify-between gap-4"><dt className="text-[#718183]">{label}</dt><dd className="text-right font-medium">{value}</dd></div>)}</dl></section>{run.failed_agents > 0 && <section className="rounded-lg border border-red-200 bg-red-50 p-5"><div className="flex items-center gap-2 text-sm font-semibold text-red-700"><AlertTriangle size={16} />存在失败任务</div><p className="mt-2 text-xs leading-5 text-red-700/80">当前后端支持复用检查点，只重新生成失败或未完成章节。</p><Button variant="outline" className="mt-4 border-red-300 bg-white text-red-700" onClick={() => void action('retry')}><RefreshCw size={14} />重试失败任务</Button></section>}<section className="rounded-lg bg-[#eef6f6] p-4 text-xs leading-5 text-[#657678]"><Clock3 size={15} className="mb-2 text-[#147d86]" />当前版本展示章节级 Agent。更细的实时步骤和中间草稿将在生成核心提供结构化回调后自动扩展。</section></aside></div></div>
}
